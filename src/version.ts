// Single source of truth for the app version string. Shown on the About page
// and in the sidebar footer. Bump this on release (keep in step with
// package.json / Tauri config when those are aligned).
export const APP_VERSION = "1.2.0";
